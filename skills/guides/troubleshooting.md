# Skills 故障排除指南

常见问题、诊断方法和解决方案集合。

## 📚 快速导航

- [Skill 无法加载](#skill-无法加载)
- [Skill 未被触发](#skill-未被触发)
- [输出不符合预期](#输出不符合预期)
- [性能问题](#性能问题)
- [跨平台问题](#跨平台问题)
- [调试工具](#调试工具)

---

## 🚫 Skill 无法加载

### 问题表现

- Claude Code 启动后看不到 Skill
- 手动指定 Skill 时提示不存在
- `/skill list` 命令中没有你的 Skill

### 诊断步骤

#### 1. 检查文件位置

```bash
# 检查 Skill 是否在正确位置
ls -la ~/.claude/skills/

# 或
ls -la ~/.config/claude-code/skills/

# 应该看到你的 Skill 目录
```

**解决方法**:
```bash
# 如果位置错误，移动到正确位置
mv /wrong/path/my-skill ~/.claude/skills/
```

#### 2. 检查文件名

```bash
# 检查文件名（必须是全大写的 SKILL.md）
ls ~/.claude/skills/my-skill/

# ❌ 错误的文件名：
# skill.md
# Skill.md
# SKILLS.md

# ✅ 正确的文件名：
# SKILL.md
```

**解决方法**:
```bash
# 重命名文件
cd ~/.claude/skills/my-skill/
mv skill.md SKILL.md
```

#### 3. 检查 YAML Frontmatter

```bash
# 查看前几行
head -10 ~/.claude/skills/my-skill/SKILL.md
```

**常见错误**:

```yaml
❌ 错误 1：缺少分隔线
name: my-skill
description: ...

✅ 正确：
---
name: my-skill
description: ...
---

❌ 错误 2：缺少冒号
---
name my-skill
description: ...
---

✅ 正确：
---
name: my-skill
description: ...
---

❌ 错误 3：使用了 Tab
---
name:	my-skill   # 这里有 Tab
---

✅ 正确（使用空格）：
---
name: my-skill
---
```

**验证工具**:
```bash
# 使用 Python 验证 YAML
python3 -c "
import yaml
with open('SKILL.md') as f:
    content = f.read()
    parts = content.split('---')
    if len(parts) >= 3:
        yaml.safe_load(parts[1])
        print('✅ YAML 格式正确')
    else:
        print('❌ 缺少 YAML frontmatter')
"
```

#### 4. 检查文件权限

```bash
# 检查文件是否可读
ls -l ~/.claude/skills/my-skill/SKILL.md

# 应该有读权限（r）
# -rw-r--r-- 是正常的

# 如果没有读权限，添加
chmod +r ~/.claude/skills/my-skill/SKILL.md
```

---

## 🎯 Skill 未被触发

### 问题表现

- Skill 已加载，但不会自动激活
- Claude 似乎没有使用 Skill 的指令

### 诊断步骤

#### 1. 检查 Description

```yaml
# Description 是触发条件的关键

❌ 太泛泛：
description: 工具

❌ 太专业化：
description: 使用高级机器学习算法分析时间序列数据并预测未来趋势

✅ 恰到好处：
description: 分析 CSV 数据并生成可视化报告，适用于销售、用户行为等场景
```

**改进建议**:
1. 包含具体的使用场景
2. 提及支持的数据类型
3. 说明输出格式
4. 保持简洁（< 100 words）

#### 2. 测试触发词

创建测试文件 `test_trigger.md`:

```markdown
# 测试 my-skill 的触发条件

## 测试 1：精确匹配
提示词：[包含 description 中的关键词]
预期：应该触发

## 测试 2：模糊匹配
提示词：[相关但不精确的描述]
预期：可能触发

## 测试 3：无关场景
提示词：[完全不相关的任务]
预期：不应触发
```

在 Claude Code 中逐个测试。

#### 3. 检查冲突

```bash
# 列出所有 Skills
ls ~/.claude/skills/

# 检查是否有相似的 Skills
grep -r "CSV" ~/.claude/skills/*/SKILL.md

# 如果多个 Skills 的 description 相似，可能会冲突
```

**解决方法**:
1. 更新 description 使其更具体
2. 禁用或删除冲突的 Skills
3. 手动指定使用哪个 Skill

#### 4. 手动触发测试

```bash
# 在 Claude Code 中
/skill my-skill

# 或在提示词中明确指定
"使用 my-skill 分析这个数据"
```

---

## 📤 输出不符合预期

### 问题表现

- Skill 被触发，但输出格式错误
- 缺少某些步骤
- 输出质量不佳

### 诊断步骤

#### 1. 检查示例质量

```markdown
❌ 不好的示例：
## 示例
用户：分析数据
输出：已分析

✅ 好的示例：
## 示例

**用户输入**:
```
分析这个销售数据 CSV 文件
[文件：sales.csv，包含日期、产品、金额列]
```

**预期输出**:
```
📊 数据摘要
- 记录数：1,234 行
- 时间范围：2024-01 到 2024-12
- 字段：date, product, amount

📈 统计分析
- 总销售额：$1,234,567
- 平均订单：$1,000
- 最畅销产品：产品 A（45% 占比）

[可视化图表]

💡 建议
1. 产品 A 表现优秀，建议增加库存
2. 12月销售额显著提升，分析促销效果
3. 产品 C 销售下滑，需要优化策略
```
```

**改进建议**:
1. 提供完整的输入示例
2. 展示详细的输出格式
3. 包含多个场景的示例
4. 添加边界情况的示例

#### 2. 强化指令

```markdown
❌ 模糊的指令：
## 指令
分析数据并生成报告

✅ 详细的指令：
## 指令

### 步骤 1: 数据接收和验证
1. 接收用户提供的 CSV 文件
2. 验证文件格式：
   - 检查必需列：date, amount
   - 验证数据类型
   - 识别编码（UTF-8/GBK）
3. 输出确认信息：`✅ 已接收文件：{filename}`

### 步骤 2: 数据分析
1. 计算基础统计
   - 记录数、时间范围
   - 总和、均值、中位数
2. 识别趋势
   - 增长率
   - 季节性模式
3. 异常值检测

### 步骤 3: 生成报告
必须包含：
- 📊 数据摘要（表格格式）
- 📈 统计分析（关键指标）
- 📉 趋势图表（可视化）
- 💡 优化建议（3-5 条）

输出格式：
```
[具体的输出模板]
```

### 步骤 4: 后续建议
提供：
- 可能的深入分析方向
- 数据质量改进建议
```

#### 3. 添加约束

```markdown
## 输出约束

### 必须遵守
- ✅ 使用 Markdown 格式
- ✅ 包含 emoji 提高可读性
- ✅ 数字使用千分位分隔符
- ✅ 提供具体的数据支持

### 禁止
- ❌ 不要编造数据
- ❌ 不要省略任何步骤
- ❌ 不要使用模糊的描述
```

---

## 🐌 性能问题

### 问题表现

- Skill 加载缓慢
- 响应时间长
- Token 使用量大

### 诊断步骤

#### 1. 检查文件大小

```bash
# 检查 SKILL.md 大小
wc -w ~/.claude/skills/my-skill/SKILL.md

# 理想：< 2000 words
# 可接受：2000-5000 words
# 需优化：> 5000 words
```

**优化方法**:
```bash
# 1. 移动详细文档到 references/
mkdir -p ~/.claude/skills/my-skill/references
# 将详细内容移到 references/ 目录

# 2. 简化示例
# 保留 3-5 个代表性示例
# 其他示例放到 references/examples.md

# 3. 移除冗余
# 删除重复的描述
# 合并相似的章节
```

#### 2. 使用渐进式加载

```markdown
## 基础功能

[简要说明]

## 高级功能

详见 [高级用法](references/advanced.md)

## API 参考

完整 API 文档：[API Reference](references/api.md)
```

#### 3. 优化 Description

```yaml
# 目标：< 100 words

# 检查当前长度
wc -w <<EOF
[粘贴你的 description]
EOF

# 如果超过 100 words，精简
```

---

## 🌐 跨平台问题

### Claude.ai 网页版问题

**问题**: Skill 在 Claude Code 工作，但在 Claude.ai 不工作

**原因**:
- Claude.ai 不支持本地 Skills
- 需要通过 UI 上传或使用官方 Skills

**解决方法**:
1. 在 Claude.ai 中手动上传 Skill
2. 或使用 Claude API

### Claude API 问题

**问题**: API 调用时 Skill 不生效

**检查**:
```python
# 确认 skills 参数正确
response = client.messages.create(
    model="claude-sonnet-4-20250514",
    max_tokens=4096,
    skills=["my-skill"],  # ← 检查这里
    messages=[{"role": "user", "content": "..."}]
)
```

**常见错误**:
```python
❌ skills=["my-skill-name"]  # 使用了完整路径
❌ skills="my-skill"          # 应该是列表
✅ skills=["my-skill"]        # 正确
```

---

## 🔧 调试工具

### 1. Skill 验证脚本

```bash
#!/bin/bash
# validate_skill.sh

SKILL_DIR=$1

echo "检查 Skill: $SKILL_DIR"
echo "========================"

# 检查目录
if [ ! -d "$SKILL_DIR" ]; then
    echo "❌ 目录不存在: $SKILL_DIR"
    exit 1
fi
echo "✅ 目录存在"

# 检查 SKILL.md
if [ ! -f "$SKILL_DIR/SKILL.md" ]; then
    echo "❌ SKILL.md 不存在"
    exit 1
fi
echo "✅ SKILL.md 存在"

# 检查 YAML frontmatter
if ! head -1 "$SKILL_DIR/SKILL.md" | grep -q "^---$"; then
    echo "❌ 缺少 YAML frontmatter 起始标记"
    exit 1
fi
echo "✅ YAML frontmatter 格式正确"

# 检查文件大小
WORDS=$(wc -w < "$SKILL_DIR/SKILL.md")
echo "📄 文件大小: $WORDS words"
if [ $WORDS -gt 5000 ]; then
    echo "⚠️  文件偏大，建议 < 5000 words"
fi

echo "========================"
echo "✅ 验证完成"
```

**使用**:
```bash
bash validate_skill.sh ~/.claude/skills/my-skill
```

### 2. 触发测试脚本

```python
# test_trigger.py
import anthropic
import sys

def test_skill_trigger(skill_name, test_prompt):
    """测试 Skill 触发"""
    client = anthropic.Anthropic()

    response = client.messages.create(
        model="claude-sonnet-4-20250514",
        max_tokens=1024,
        skills=[skill_name],
        messages=[{"role": "user", "content": test_prompt}]
    )

    print(f"Skill: {skill_name}")
    print(f"Prompt: {test_prompt}")
    print(f"Response: {response.content[0].text[:200]}...")
    print("=" * 50)

if __name__ == "__main__":
    skill = sys.argv[1]
    test_skill_trigger(skill, "使用这个 skill 处理数据")
```

**使用**:
```bash
python test_trigger.py my-skill
```

---

## 📋 快速检查清单

```markdown
## Skill 问题检查清单

### 文件和结构
- [ ] 文件位置：~/.claude/skills/my-skill/
- [ ] 文件名：SKILL.md（全大写）
- [ ] 目录权限：可读

### YAML Frontmatter
- [ ] 有起始和结束标记（---）
- [ ] name 字段正确
- [ ] description < 100 words
- [ ] 使用空格（非 Tab）
- [ ] 冒号后有空格

### 内容质量
- [ ] Description 明确具体
- [ ] 包含 3+ 个示例
- [ ] 指令详细清晰
- [ ] 文件 < 5k words

### 测试
- [ ] 在 Claude Code 中加载
- [ ] 触发条件测试
- [ ] 输出格式检查
- [ ] 边界情况测试
```

---

## 🆘 获取帮助

### 社区资源

- [Claude Code Discord](https://discord.gg/anthropic)
- [GitHub Discussions](https://github.com/anthropics/claude-code/discussions)
- [awesome-claude-skills](https://github.com/ComposioHQ/awesome-claude-skills)

### 报告 Bug

提交 Issue 时请包含：
1. Skill 的 YAML frontmatter
2. 重现步骤
3. 预期行为 vs 实际行为
4. 系统环境（OS、Claude Code 版本）

---

## 📚 延伸阅读

- [创建自定义 Skills](creating-custom-skills.md)
- [Skills 最佳实践](best-practices.md)
- [编程使用 Skills](programming-usage.md)

---

**返回**: [指南目录](README.md) | [主页](../README.md)

**最后更新**: 2026-01-24
