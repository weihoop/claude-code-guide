# Claude Code 快速参考卡

一页纸速查：Claude Code 开发的核心实践和配置清单。

---

## 📋 核心文件清单

| 文件 | 位置 | 是否提交 | 优先级 | 用途 |
|------|------|---------|--------|------|
| `.claude.md` | 项目根目录 | 看情况 | ⭐⭐⭐⭐⭐ | 项目上下文和架构 |
| `settings.json` | `.claude/` | ✅ 是 | ⭐⭐⭐⭐⭐ | 团队权限配置 |
| `settings.local.json` | `.claude/` | ❌ 否 | ⭐⭐⭐⭐ | 个人权限覆盖 |
| `commands/*.md` | `.claude/` | ✅ 是 | ⭐⭐⭐⭐ | 自定义命令 |
| `SPEC.md` | 根目录 | ✅ 是 | ⭐⭐⭐⭐ | 功能规格 |
| `CHANGELOG.md` | 根目录 | ✅ 是 | ⭐⭐⭐⭐ | 变更日志 |
| `CONTRIBUTING.md` | 根目录 | ✅ 是 | ⭐⭐⭐ | 贡献指南 |
| `.env.example` | 根目录 | ✅ 是 | ⭐⭐⭐⭐ | 环境变量模板 |

---

## 🔧 必备自定义命令

| 命令 | 别名 | 用途 | 优先级 |
|------|------|------|--------|
| `/test` | `/t` | 运行测试并分析结果 | ⭐⭐⭐⭐⭐ |
| `/build` | `/b` | 构建项目并检查错误 | ⭐⭐⭐⭐⭐ |
| `/review` | `/cr` | 代码审查流程 | ⭐⭐⭐⭐⭐ |
| `/push` | `/p` | 快速提交推送 | ⭐⭐⭐⭐ |
| `/deploy` | - | 部署到指定环境 | ⭐⭐⭐⭐ |
| `/fix` | - | 自动修复代码问题 | ⭐⭐⭐⭐ |
| `/doc` | - | 更新文档 | ⭐⭐⭐ |
| `/changelog` | - | 生成变更日志 | ⭐⭐⭐ |

---

## 🔐 权限配置速查

### 推荐的 allow（常用操作）
```json
"allow": [
  "Read",
  "Edit(/src/**)",
  "Glob",
  "Grep",
  "Bash(npm run dev:*)",
  "Bash(npm run test:*)",
  "Bash(git status:*)",
  "Bash(git diff:*)",
  "Bash(git add:*)",
  "Bash(git commit:*)",
  "WebSearch"
]
```

### 推荐的 ask（需确认）
```json
"ask": [
  "Edit(/package.json)",
  "Write(/src/**)",
  "Bash(git push:*)",
  "Bash(npm install:*)",
  "Bash(npm publish:*)"
]
```

### 推荐的 deny（禁止）
```json
"deny": [
  "Read(.env)",
  "Read(.env.local)",
  "Edit(/node_modules/**)",
  "Bash(rm -rf:*)",
  "Bash(sudo:*)",
  "Bash(git push --force:*)"
]
```

---

## 📝 .claude.md 最小模板

```markdown
# 项目名称

## 项目概述
- **类型**：Web应用/API/CLI/库
- **技术栈**：Next.js 15, TypeScript, PostgreSQL
- **用途**：简短描述

## 项目结构
\```
src/
├── app/          # 应用代码
├── components/   # 组件
└── lib/          # 工具库
\```

## 常用命令
\```bash
npm run dev      # 开发
npm test         # 测试
npm run build    # 构建
\```

## 开发规范
- **代码风格**：TypeScript 严格模式
- **测试覆盖率**：≥ 80%
- **提交格式**：feat/fix/docs: 描述
```

---

## 🚀 典型工作流

### 1. 项目初始化
```bash
claude
> /init
> 创建 Next.js + TypeScript 项目配置
```

### 2. 开发新功能
```
> 开发用户认证功能
↓
Claude 自动：设计 → 实现 → 测试 → 文档
```

### 3. 代码审查
```bash
> /review src/auth/login.ts
```

### 4. 测试和构建
```bash
> /test
> /build
```

### 5. 发布版本
```bash
> /changelog
> 生成 v2.0.0 变更日志
```

### 6. 提交推送
```bash
> /push
```

---

## 📐 SPEC.md 核心结构

```markdown
# 功能规格

## 功能需求
### 1. 功能名称
- 功能描述
- 输入/输出
- 验证规则
- 测试用例

## 数据模型
\```typescript
interface User {
  id: string;
  email: string;
}
\```

## API 设计
### POST /api/endpoint
- 请求格式
- 响应格式
- 错误码

## 技术实现
- 实现流程
- 安全措施
```

---

## 📅 CHANGELOG.md 格式

```markdown
# Changelog

## [Unreleased]
### Added
- 新功能

### Fixed
- 修复的问题

## [2.0.0] - 2025-01-10
### Added
- 重大新功能

### Changed
- 重要变更

### Security
- 安全修复
```

---

## ✅ Git 提交规范

| 类型 | 说明 | 示例 |
|------|------|------|
| `feat` | 新功能 | `feat: 添加用户认证` |
| `fix` | 修复 | `fix: 修复登录问题` |
| `docs` | 文档 | `docs: 更新 API 文档` |
| `style` | 格式 | `style: 代码格式化` |
| `refactor` | 重构 | `refactor: 优化查询逻辑` |
| `test` | 测试 | `test: 添加单元测试` |
| `chore` | 其他 | `chore: 更新依赖` |

---

## 🎯 测试目录结构

```
tests/
├── unit/              # 单元测试
├── integration/       # 集成测试
├── e2e/              # 端到端测试
├── fixtures/         # 测试数据
└── setup.ts          # 测试配置
```

**测试命令**：
```json
{
  "test": "jest",
  "test:watch": "jest --watch",
  "test:unit": "jest tests/unit",
  "test:e2e": "playwright test",
  "test:coverage": "jest --coverage"
}
```

---

## 🛡️ 安全检查清单

- [ ] `.env` 文件不提交到 Git
- [ ] 禁止读取敏感文件（`deny: ["Read(.env*)"]`）
- [ ] 禁止破坏性命令（`deny: ["Bash(rm -rf:*)"]`）
- [ ] 重要操作需确认（`ask: ["Bash(git push:*)"]`）
- [ ] API 密钥通过环境变量注入
- [ ] 所有用户输入必须验证
- [ ] 使用参数化查询防止 SQL 注入
- [ ] 实施速率限制

---

## 💡 最佳实践速记

### ✅ 必做
1. 项目初始化时创建 `.claude.md`
2. 配置严格的权限控制
3. 使用自定义命令标准化工作流
4. 保持 SPEC 和代码同步
5. 每次发布更新 CHANGELOG
6. 提交前运行 `/review`

### ❌ 禁止
1. 不写项目文档
2. 权限配置过于宽松
3. 跳过测试直接提交
4. 硬编码敏感信息
5. 手动重复执行命令
6. 忘记更新 CHANGELOG

---

## 📚 完整文档

详细内容请查看：
- [完整最佳实践指南](./claude-code-best-practices.md)
- [基础使用手册](../basic-guide.md)
- [进阶使用手册](../advanced-guide.md)
- [安全使用手册](../security-guide.md)

---

## 🔗 快速链接

- [Claude Code 官方文档](https://code.claude.com/docs/)
- [Keep a Changelog](https://keepachangelog.com/)
- [Semantic Versioning](https://semver.org/)
- [Conventional Commits](https://www.conventionalcommits.org/)
