# Claude Code 基础使用手册

> 本手册适合初次使用 Claude Code 的用户，涵盖所有基础功能和常用操作

---

## 📋 目录

- [安装和启动](#安装和启动)
- [基本界面](#基本界面)
- [核心概念](#核心概念)
- [基础配置](#基础配置)
- [文件操作](#文件操作)
- [Mode 切换](#mode-切换)
- [基本命令](#基本命令)
- [权限管理](#权限管理)
- [常见任务](#常见任务)

---

## 🚀 安装和启动

### 系统要求

- **操作系统**: macOS, Linux, Windows
- **Python**: 3.8+ (如使用 Python 项目)
- **Node.js**: 14+ (如使用 Node.js 项目)
- **终端**: 支持 ANSI 颜色的现代终端

### 安装步骤

```bash
# 方法 1: 使用 npm 安装（推荐）
npm install -g @anthropic-ai/claude-code

# 方法 2: 使用 homebrew (macOS)
brew install claude-code

# 方法 3: 下载二进制文件
# 访问 https://claude.com/claude-code 下载
```

### 验证安装

```bash
# 查看版本
claude --version

# 查看帮助
claude --help
```

### 启动 Claude Code

```bash
# 在当前目录启动
claude

# 在指定项目目录启动
claude /path/to/project

# 带特定模型启动
claude --model sonnet
```

---

## 🖥️ 基本界面

### 界面组成

```
┌─────────────────────────────────────────┐
│  Claude Code v1.0.0                     │  ← 状态栏
├─────────────────────────────────────────┤
│  Working directory: /path/to/project    │  ← 工作目录
│  Model: claude-sonnet-4-5               │  ← 当前模型
├─────────────────────────────────────────┤
│                                         │
│  > 你的对话内容...                       │  ← 对话区域
│                                         │
│  Claude 的回复...                        │
│                                         │
├─────────────────────────────────────────┤
│  > _                                    │  ← 输入区域
└─────────────────────────────────────────┘
```

### 快捷键

| 快捷键 | 功能 |
|--------|------|
| `Ctrl+C` | 中断当前操作 |
| `Ctrl+D` | 退出 Claude Code |
| `Ctrl+L` | 清屏 |
| `↑` / `↓` | 浏览历史命令 |
| `/` + `Tab` | 自动补全斜杠命令 |

---

## 🧠 核心概念

### 1. 工具 (Tools)

Claude Code 通过工具与系统交互：

| 工具 | 功能 | 示例 |
|------|------|------|
| `Read` | 读取文件 | 查看源代码 |
| `Edit` | 编辑文件 | 修改代码 |
| `Write` | 创建文件 | 生成新文件 |
| `Bash` | 执行命令 | 运行测试、构建 |
| `Grep` | 搜索代码 | 查找函数定义 |
| `Glob` | 查找文件 | 匹配文件模式 |
| `WebFetch` | 获取网页 | 查询文档 |
| `Task` | 启动子任务 | 复杂任务分解 |

### 2. 上下文 (Context)

Claude Code 会自动管理对话上下文：
- **当前目录**: 工作目录中的文件
- **已读文件**: 之前读取的文件内容
- **对话历史**: 最近的对话记录
- **项目配置**: `.claude/settings.json` 等

### 3. 权限 (Permissions)

工具访问需要权限控制：
- **Allow**: 自动允许
- **Ask**: 需要确认
- **Deny**: 完全拒绝

详见：[权限配置完整指南](docs/configuration/permissions.md)

---

## ⚙️ 基础配置

### 全局配置文件

**位置**: `~/.claude/settings.json`

**基本配置示例**:

```json
{
  "model": "claude-sonnet-4-5",
  "permissions": {
    "allow": [
      "Read",
      "Grep",
      "Glob",
      "Bash(git status:*)",
      "Bash(npm run test:*)"
    ],
    "ask": [
      "Edit",
      "Write",
      "Bash(git commit:*)"
    ],
    "deny": [
      "Bash(sudo:*)",
      "Bash(rm:*)",
      "Read(.env*)"
    ]
  }
}
```

### 项目配置文件

**位置**: `.claude/settings.json` (团队共享)

**示例**:

```json
{
  "permissions": {
    "allow": [
      "Bash(npm run dev:*)",
      "Edit(/src/**)"
    ],
    "deny": [
      "Edit(/node_modules/**)"
    ]
  }
}
```

### 配置优先级

```
项目本地配置 > 项目共享配置 > 全局配置
```

---

## 📁 文件操作

### 读取文件

```
> 帮我看看 src/index.js 文件
```

Claude 会使用 `Read` 工具读取文件并分析。

**技巧**:
- 可以一次读取多个文件
- 支持通配符：`读取 src/**/*.js 中的所有文件`

### 编辑文件

```
> 在 src/utils.js 中添加一个 formatDate 函数
```

Claude 会：
1. 读取文件（如果还没读取）
2. 使用 `Edit` 工具进行修改
3. 显示修改内容供确认

**注意**:
- 编辑前会显示差异对比
- 可以要求撤销或重新编辑

### 创建文件

```
> 创建一个 README.md 文件，包含项目说明
```

Claude 使用 `Write` 工具创建新文件。

**技巧**:
- 可以指定文件路径
- 可以基于模板创建

### 查找文件

```
> 找到所有的测试文件
> 查找包含 "API" 的文件
```

Claude 使用 `Glob` 或 `Grep` 工具搜索。

---

## 🔄 Mode 切换

### Plan Mode (规划模式)

**何时使用**:
- 开始新功能开发前
- 需要重构代码
- 解决复杂问题

**如何进入**:

```
> 我想添加用户认证功能，先帮我规划一下
```

或使用工具触发（Claude 会自动判断）。

**Plan Mode 特点**:
- 只读取和分析代码
- 不会修改文件
- 生成实现计划
- 需要用户确认后才执行

**示例**:

```
User: 帮我添加用户登录功能

Claude: 我来帮你规划这个功能的实现：

【规划阶段】
1. 分析现有代码结构
2. 设计认证流程
3. 确定需要修改的文件

【实现步骤】
1. 创建 auth.js 处理认证逻辑
2. 修改 routes.js 添加登录路由
3. 更新数据库 schema
4. 编写单元测试

是否开始实施？
```

### Build Mode (构建模式)

**何时使用**:
- 执行具体的代码修改
- 创建新文件
- 运行测试和构建

**如何进入**:

在 Plan Mode 确认后自动进入，或直接请求：

```
> 帮我在 utils.js 中添加一个日期格式化函数
```

**Build Mode 特点**:
- 直接修改代码
- 创建文件
- 运行命令
- 实时反馈结果

### Mode 切换技巧

```
# 明确要求规划
> 先别改代码，帮我规划一下怎么重构这个模块

# 直接执行
> 直接帮我修复这个 bug

# 查看当前 Mode
> 我们现在在什么模式？
```

---

## 🔧 基本命令

### 内置斜杠命令

| 命令 | 功能 | 示例 |
|------|------|------|
| `/help` | 显示帮助 | `/help` |
| `/permissions` | 管理权限 | `/permissions` |
| `/context` | 查看上下文 | `/context` |
| `/model` | 查看/切换模型 | `/model` |
| `/clear` | 清除对话 | `/clear` |
| `/exit` | 退出 | `/exit` |

### 系统命令

```bash
# 查看当前状态
/context

# 切换模型
/model sonnet    # 使用 Sonnet
/model opus      # 使用 Opus (更强大)
/model haiku     # 使用 Haiku (更快速)

# 管理权限
/permissions

# 清除对话历史（释放 token）
/clear
```

---

## 🔐 权限管理

### 基础权限配置

**推荐新手配置**:

```json
{
  "permissions": {
    "allow": [
      "Read",
      "Grep",
      "Glob"
    ],
    "ask": [
      "Edit",
      "Write",
      "Bash"
    ]
  }
}
```

这样配置：
- ✅ 可以自由读取和搜索
- ✅ 修改文件前需要确认
- ✅ 执行命令前需要确认
- ✅ 安全性高

### 查看和修改权限

```bash
# 打开权限管理界面
/permissions

# 或编辑配置文件
nano ~/.claude/settings.json
```

### 常见权限场景

#### 1. 安全的只读模式

```json
{
  "permissions": {
    "allow": ["Read", "Grep", "Glob"],
    "ask": ["Edit", "Write", "Bash"]
  }
}
```

#### 2. 开发模式（宽松）

```json
{
  "permissions": {
    "allow": [
      "Read", "Edit", "Grep", "Glob",
      "Bash(npm:*)", "Bash(git:*)"
    ],
    "ask": ["Write", "Bash(git push:*)"],
    "deny": ["Bash(sudo:*)", "Bash(rm:*)"]
  }
}
```

#### 3. 生产模式（严格）

```json
{
  "permissions": {
    "allow": ["Read(/src/**)", "Grep", "Glob"],
    "ask": ["Edit(/src/**)", "Bash(git:*)"],
    "deny": ["Write", "Bash(rm:*)", "Read(.env*)"]
  }
}
```

完整权限配置请参考：[权限配置完整指南](docs/configuration/permissions.md)

---

## 📝 常见任务

### 1. 代码审查

```
> 帮我审查一下 src/api.js 的代码质量
```

Claude 会：
- 读取文件
- 检查代码风格
- 发现潜在问题
- 提供改进建议

### 2. 调试问题

```
> 我运行测试时报错：TypeError: Cannot read property 'name' of undefined
> 帮我找出问题
```

Claude 会：
- 分析错误信息
- 读取相关文件
- 定位问题原因
- 提供修复方案

### 3. 添加功能

```
> 帮我在用户模块添加邮箱验证功能
```

Claude 会：
1. 规划实现方案
2. 读取相关文件
3. 编写新代码
4. 更新相关测试

### 4. 重构代码

```
> 帮我把 utils.js 中的函数拆分成多个小模块
```

Claude 会：
1. 分析现有结构
2. 设计新的模块划分
3. 创建新文件
4. 迁移代码
5. 更新引用

### 5. 编写测试

```
> 为 auth.js 编写单元测试
```

Claude 会：
- 读取源代码
- 识别需要测试的函数
- 编写测试用例
- 使用合适的测试框架

### 6. 生成文档

```
> 为这个项目生成 README.md
```

Claude 会：
- 分析项目结构
- 识别主要功能
- 生成使用说明
- 添加示例代码

### 7. 运行命令

```
> 帮我运行测试
> 构建项目
> 检查代码风格
```

Claude 会使用 `Bash` 工具执行相应命令。

---

## 💡 使用技巧

### 1. 清晰的指令

**好的指令**:
```
✅ 在 src/utils.js 中添加一个 formatCurrency 函数，接受数字返回货币格式
✅ 修复 login.js 第 42 行的空指针异常
✅ 重构 UserService 类，拆分成更小的模块
```

**不好的指令**:
```
❌ 改一下代码
❌ 有 bug
❌ 不太好
```

### 2. 分步执行

对于复杂任务，分步进行：

```
# 第 1 步：规划
> 我想添加用户认证，先帮我规划一下

# 第 2 步：确认后执行
> 好的，开始实施

# 第 3 步：测试
> 帮我编写测试用例

# 第 4 步：验证
> 运行测试看看是否通过
```

### 3. 提供上下文

```
✅ 我在使用 React + TypeScript，帮我创建一个用户列表组件
✅ 这个项目使用 Jest 测试框架，帮我添加测试
✅ 我们的 API 返回 JSON 格式，帮我解析响应数据
```

### 4. 利用历史记录

Claude 记住之前的对话：

```
> 帮我看看 auth.js

（Claude 读取文件）

> 在这个文件中添加密码加密功能

（Claude 知道是哪个文件，不需要重新读取）
```

### 5. 确认重要操作

对于重要修改，要求先展示：

```
> 先不要修改，展示给我看看你打算怎么改
```

---

## 🐛 常见问题

### Q1: Claude 读取了错误的文件

**解决方案**:
```
> 不是这个文件，是 src/components/User.js
```

### Q2: 修改不符合预期

**解决方案**:
```
> 撤销刚才的修改
> 或：重新修改，这次要...
```

### Q3: 命令执行失败

**解决方案**:
1. 检查权限配置
2. 使用 `/permissions` 查看规则
3. 临时允许：修改 `.claude/settings.local.json`

### Q4: Token 使用过快

**解决方案**:
1. 使用 `/clear` 清除历史
2. 避免读取大文件
3. 使用更小的模型（Haiku）
4. 详见：[最佳实践 - Token 优化](best-practices.md#token-优化)

### Q5: 找不到项目文件

**解决方案**:
```bash
# 确认工作目录
pwd

# 重新启动到正确目录
cd /path/to/project
claude
```

---

## 🎯 下一步

掌握基础后，可以学习：

- **[进阶使用手册](advanced-guide.md)** - Skill、MCP、Hooks 等高级功能
- **[最佳实践](best-practices.md)** - Token 优化、团队协作、安全性
- **[权限配置指南](docs/configuration/permissions.md)** - 深入理解权限系统
- **[官方资源](docs/resources/official-resources.md)** - 官方文档和社区

---

## 📚 相关资源

- [主文档](README.md)
- [进阶使用手册](advanced-guide.md)
- [最佳实践](best-practices.md)
- [权限配置指南](docs/configuration/permissions.md)
- [官方资源](docs/resources/official-resources.md)

---

**最后更新**: 2025-11-27
**文档版本**: 1.0
**适用于**: Claude Code 所有版本
